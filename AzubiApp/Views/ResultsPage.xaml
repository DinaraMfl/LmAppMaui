<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:resx="clr-namespace:AzubiApp.Resources.Translate"
             x:Class="AzubiApp.Views.ResultsPage"
             BackgroundColor="Black">
    <ScrollView>

        <VerticalStackLayout Padding="20">

            <Image x:Name="LogoImage" Source="logo_white.png" HeightRequest="50" WidthRequest="200" HorizontalOptions="Start" />
            <Label x:Name="ResultTitle" Text="{x:Static resx:AppResources.ResultTitle}" FontSize="38" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" Margin="0,60,0,20"/>

            <!-- Label to display the total number of correct answers -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="5">
                <Label x:Name="CorrectAnswerTitle" FontSize="18" TextColor="White" Text="Score:"/>
                <Label x:Name="ScoreLabel" FontSize="18" TextColor="White" />
            </HorizontalStackLayout>

            <CollectionView ItemsSource="{Binding Results}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>

                        <Frame BorderColor="Black" Padding="10" Margin="5" BackgroundColor="#E8E9E8" CornerRadius="20">
                            <VerticalStackLayout>
                                <toolkit:Expander>

                                    <toolkit:Expander.Header>
                                        <VerticalStackLayout Padding="5">
                                            <Grid ColumnDefinitions="*,Auto" VerticalOptions="Center">
                                                <!-- Text on the left -->
                                                <Label Grid.Column="0"
                                                       Text="{Binding QuestionText}"
                                                       FontAttributes="Bold"
                                                       TextColor="Black"
                                                       VerticalTextAlignment="Center" />

                                                <!-- Dot on the right -->
                                                <Frame Grid.Column="1"
                                                       BackgroundColor="{Binding ResultColor}"
                                                       CornerRadius="50"
                                                       HeightRequest="10"
                                                       WidthRequest="10"
                                                       Margin="90,0,0,0"
                                                       VerticalOptions="Center"
                                                       HorizontalOptions="End" />
                                            </Grid>
                                        </VerticalStackLayout>
                                    </toolkit:Expander.Header>

                                    <toolkit:Expander.Content>
                                        <VerticalStackLayout Padding="5">
                                            <Label Text="{Binding UserAnswerText}" TextColor="Black" FontAttributes="Bold" Margin="0,0,100,0"/>
                                            <Label Text="{Binding CorrectAnswerText}" TextColor="DarkGreen" IsVisible="{Binding ShowCorrectAnswer}" FontAttributes="Bold"/>
                                        </VerticalStackLayout>
                                    </toolkit:Expander.Content>

                                </toolkit:Expander>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button x:Name="BackToStartButton" Text="{x:Static resx:AppResources.BackToStartButton}"
                BackgroundColor="#FFCD00"
                TextColor="Black"
                Clicked="OnBackToStartClicked"
                CornerRadius="20"
                FontSize="18"
                WidthRequest="200"
                Margin="0,30,0,0"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
